
==========
== CUDA ==
==========

CUDA Version 13.2.0

Container image Copyright (c) 2016-2023, NVIDIA CORPORATION & AFFILIATES. All rights reserved.

This container image and its contents are governed by the NVIDIA Deep Learning Container License.
By pulling and using the container, you accept the terms and conditions of this license:
https://developer.nvidia.com/ngc/nvidia-deep-learning-container-license

A copy of this license is made available in this container at /NGC-DL-CONTAINER-LICENSE for your convenience.

{
  "definition": "fused_add_rmsnorm_h4096",
  "use_mixture": false,
  "k_expert": 5,
  "best_auc_visible": 1.0,
  "best_speedup_visible": 3.0373726215272043,
  "final_auc_heldout": 1.0,
  "roofline_tier": 1,
  "gpu_clock_mhz": 2418,
  "utilization": {
    "achieved_tflops": 0.18546393345440582,
    "achieved_gbps": 222.57483185753838,
    "pct_of_hw_peak": 0.8152924243865874,
    "pct_of_cublas": 1.0371811093627985,
    "peak_basis": "GB/s (hw=273 est.)"
  },
  "iterations": [
    {
      "iter": 0,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: CompilationError: at 29:8:\n        y_ptr: Pointer to output tensor (M, H), dtype float32.\n       ",
      "used_expert": false
    },
    {
      "iter": 1,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: TypeError: 'tuple' object cannot be interpreted as an integer",
      "used_expert": false
    },
    {
      "iter": 2,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: CompilationError: at 17:8:\n    H,\n    eps,\n    BLOCK_H: tl.constexpr,\n    num_stages: tl.constexp",
      "used_expert": false
    },
    {
      "iter": 3,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: INCORRECT_NUMERICAL matched_ratio=0.0645 < 1.0 (max_abs=10.56, max_rel=1.089)",
      "used_expert": false
    },
    {
      "iter": 4,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: CompilationError: at 17:8:\n    H,\n    eps,\n    BLOCK_H: tl.constexpr,\n    num_stages: tl.constexp",
      "used_expert": false
    },
    {
      "iter": 5,
      "decision": "revert",
      "reason": "smoke norm M=64 seed0: CompilationError: at 23:10:\n    cols = tl.arange(0, BLOCK_H)\n    mask = cols < H\n\n    # Load inpu",
      "used_expert": true
    },
    {
      "iter": 6,
      "decision": "revert",
      "auc": 1.0,
      "used_expert": false
    }
  ],
  "final_source": "\"\"\"Fused add+RMSNorm seed kernel \u2014 one program per row, single HBM pass.\n\ny = rmsnorm(x + residual) * weight, with fp32 accumulation of the sum-of-squares.\nEager PyTorch runs this as ~5 unfused ATen kernels (add, pow, mean, rsqrt, mul, mul)\nwith repeated HBM round-trips; fusing into one Triton pass is the memory-bound win\n(cf. AutoKernel RMSNorm 5.29x over eager). This seed is a straightforward fused\nversion; the tuner's job is to push it toward the LPDDR5X bandwidth roofline.\n\"\"\"\n\nimport torch\nimport triton\nimport triton.language as tl\n\n\n@triton.jit\ndef _fused_add_rmsnorm(x_ptr, res_ptr, w_ptr, y_ptr, H, eps, BLOCK_H: tl.constexpr):\n    row = tl.program_id(0)\n    cols = tl.arange(0, BLOCK_H)\n    mask = cols < H\n    off = row * H + cols\n    x = tl.load(x_ptr + off, mask=mask, other=0.0).to(tl.float32)\n    res = tl.load(res_ptr + off, mask=mask, other=0.0).to(tl.float32)\n    h = x + res\n    var = tl.sum(h * h, axis=0) / H\n    rstd = 1.0 / tl.sqrt(var + eps)\n    w = tl.load(w_ptr + cols, mask=mask, other=0.0).to(tl.float32)\n    y = h * rstd * w\n    tl.store(y_ptr + off, y.to(y_ptr.dtype.element_ty), mask=mask)\n\n\ndef run(x, residual, weight):\n    M, H = x.shape\n    y = torch.empty_like(x)\n    _fused_add_rmsnorm[(M,)](\n        x, residual, weight, y, H, 1e-6,\n        BLOCK_H=triton.next_power_of_2(H), num_warps=8,\n    )\n    return y\n"
}
